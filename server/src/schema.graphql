  type Link {
    id: ID!
    descrinption: String!
    url: String!
  }

  type Query {
    info:  String!
    feed: [Link!]!
  }

  type Mutation {
    post(url: String!, descrinption: String!): Link!
  }